{
  "name": "Template Connected2Fiber Application",
  "version": "1.0.0",
  "description": "Template Application",

  "scripts": {
    "start": "NODE_ENV=uat ts-node src/app.ts",
    "build": "rm -rf ./dist && npx tsc",
    "install-dependencies": "npm i aevm bcrypt bluebird body config cors dotenv express jsonwebtoken joi lodash moment morgan pg promise ts typescript && npm i -D @types/bcrypt @types/bluebird @types/config @types/elasticsearch @types/express @types/jsonwebtoken @types/lodash @types/moment @types/node tslint",

    "debug": "NODE_ENV=uat node --inspect -r ts-node/register src/app.ts",
    "pm2:uat": "npm run build && pm2 start ecosystem.config.js --env uat",
    "pm2:prod": "npm run build && pm2 start ecosystem.config.js --env production"
  },

  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
  }
}
