{
  "name": "Template_Application",
  "version": "1.0.0",
  "description": "Template Application",

  "scripts": {
    "start": "NODE_ENV=uat ts-node src/app.ts",
    "build": "rm -rf ./dist && npx tsc",
    "debug": "NODE_ENV=uat node --inspect -r ts-node/register src/app.ts",
    "pm2:uat": "npm run build && pm2 start ecosystem.config.js --env uat",
    "pm2:prod": "npm run build && pm2 start ecosystem.config.js --env prod"
  },

  "author": "",
  "license": "ISC",
  "dependencies": {
    "aevm": "*",
    "bcrypt": "*",
    "bluebird": "*",
    "body-parser": "*",
    "config": "*",
    "cors": "*",
    "dotenv": "*",
    "express": "*",
    "jsonwebtoken": "*",
    "lodash": "*",
    "moment": "*",
    "morgan": "*",
    "pg": "*",
    "promise-mysql": "*",
    "ts-node": "*",
    "typescript": "*"
  },
  "devDependencies": {
    "@types/bcrypt": "*",
    "@types/bluebird": "*",
    "@types/config": "*",
    "@types/elasticsearch": "*",
    "@types/express": "*",
    "@types/jsonwebtoken": "*",
    "@types/lodash": "*",
    "@types/moment": "*",
    "@types/node": "*",
    "tslint": "*",
    "tslint-config-standard": "*"
  }
}
